const {assert} = require('chai');

describe('Test Example', () => {
    describe('example', () => {
        it('true is true', async () => {
            assert.equal(true, true);
        });
    });
});
